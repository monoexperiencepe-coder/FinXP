/**
 * Ancho máximo del contenido principal (pestañas).
 * En pantallas grandes / web evita que el UI se estire horizontalmente
 * y mantiene lectura y toques similares a un móvil.
 */
export const APP_CONTENT_MAX_WIDTH = 480;
